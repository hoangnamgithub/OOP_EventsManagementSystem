<UserControl x:Class="OOP_EventsManagementSystem.View.Event"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OOP_EventsManagementSystem.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:styles="clr-namespace:OOP_EventsManagementSystem.Styles"
             xmlns:vm="clr-namespace:OOP_EventsManagementSystem.ViewModel"
             DataContext="{Binding Source={StaticResource Locator}, Path=EventVM}"
             mc:Ignorable="d" 
             d:DesignHeight="555" d:DesignWidth="850"
             >
    <Grid >
        <materialDesign:Card
>
            <TabControl
   HorizontalContentAlignment="Left" Margin="5,0,0,0"
  >
                <TabItem
                       Header="Up Coming">
                    <ScrollViewer Background="White" Margin="0,5,0,0">
                        <ItemsControl>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <styles:EventDetailCard_v2 Margin="5,10,10,10"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsSource>
                                <x:Array Type="{x:Type sys:Object}">
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                </x:Array>
                            </ItemsControl.ItemsSource>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem
     Header="Happening">
                    <ScrollViewer>
                        <ItemsControl>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Height="130" Width="240" RadiusX="16" RadiusY="16"
      Fill="DarkKhaki" Margin="12" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsSource>
                                <x:Array Type="{x:Type sys:Object}">
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                </x:Array>
                            </ItemsControl.ItemsSource>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem
     Header="Completed">
                    <ScrollViewer>
                        <ItemsControl>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Height="130" Width="240" RadiusX="16" RadiusY="16"
      Fill="LightPink" Margin="12" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsSource>
                                <x:Array Type="{x:Type sys:Object}">
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                    <sys:Object />
                                </x:Array>
                            </ItemsControl.ItemsSource>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>

            </TabControl>
        </materialDesign:Card>
        <Grid>
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="2">
                <Rectangle Fill="#D6DC82" RadiusX="20" RadiusY="20" Margin="50,10,165,510" />
                <TextBlock Text="{Binding CurrentDate, StringFormat={}{0:dd MMM}}" 
           Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" 
           Margin="50,10,165,510" FontSize="18" FontFamily="Bahnschrift SemiBold" />


                <!-- Sử dụng Button UserControl -->
                <styles:Button ButtonText="Add" ButtonBackground="#D96073"
                       VerticalAlignment="Top" HorizontalAlignment="Right"
                       Margin="0,5,50,0"
                       ButtonCommand="{Binding AddCommand}" />

            </Grid>
        </Grid>
    </Grid>
</UserControl>
