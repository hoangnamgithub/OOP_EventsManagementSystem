<Page x:Class="OOP_EventsManagementSystem.View.Employee"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OOP_EventsManagementSystem.View"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="645" d:DesignWidth="1030"
      Title="Employee">
 
    <Grid Width="850" Height="500" Background="AliceBlue">
        <!-- Rectangle added -->
        <Rectangle 
                Height="200" 
                Fill="#d6dc82" 
                HorizontalAlignment="Stretch"
                Margin="0,40,0,150" Panel.ZIndex="0"
                />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="70*"/>
                <ColumnDefinition Width="45*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="70*"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>
                <Border CornerRadius="16" Grid.Row="1" Background="White">
                    <Grid>
                        <DataGrid Name="employee_table" Background="Transparent" d:ItemsSource="{d:SampleData ItemCount=10}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        
                    </DataGrid>
                </Grid>
                    
                </Border>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="60*"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="1">
                    <Border CornerRadius="16" Background="#c6c3f2" Margin="10,0,10,0">
                        <Rectangle VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

                        </Rectangle>

                    </Border>
                    <TextBlock FontSize="16" FontWeight="Bold" Text="Total" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="25,0,0,0" />
                </Grid>               
                <Grid Grid.Row="2">
                    <Border CornerRadius="16" Background="#c6c3f2" Margin="10,10,10,115">
                        <Rectangle VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        </Rectangle>
                    </Border>
                        <Grid>
                            <StackPanel Margin="20">
                            <TextBlock Text="Name"/>
                            <Border CornerRadius="16" Background="White">
                                <TextBox Name="Name_" BorderThickness="0,0,0,0"/>
                            </Border>
                            <TextBlock Text="Contact"/>
                            <Border CornerRadius="16" Background="White">
                                <TextBox BorderThickness="0,0,0,0"/>
                            </Border>
                                <TextBlock Text="Role"/>
                            <Border CornerRadius="16" Background="White">
                                <TextBox BorderThickness="0,0,0,0"/>
                            </Border>
                                <TextBlock Text="Account"/>
                            <Border CornerRadius="16" Background="White">
                                <TextBox BorderThickness="0,0,0,0"/>
                            </Border>
                            <Border>
                                <TextBlock Text="Event list" HorizontalAlignment="Right" Margin="10" TextDecorations="Underline"/>
                            </Border>
                        </StackPanel>
                        
                    </Grid>

                </Grid>
                
            </Grid>

        </Grid>
    </Grid>
</Page>
